void main(List<String> args) {
  // var numbers = [1,2,3];
  // numbers.forEach((numbers){
  //   print(numbers);
  // });

  // var person = {'name': "ghani", "age":19};
  // for (var entry in person.entries) {
  //   var key = entry.key;
  //   var value  = entry.value ;


  //  print('key $key value $value');

  var perosn = {"name": "Ghani", "age ": 19};
  switch (perosn) {
      
    case {"name": var nam, "age": var age}:
    print('Name is $nam age is $age');    
      break;
    default:
    print("Not matched");
  }
  }
